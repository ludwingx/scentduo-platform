/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "via.placeholder.com", "aromadrop.mu"],
  },
  // You can add other Next.js config options here as needed
};

module.exports = nextConfig;
